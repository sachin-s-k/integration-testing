spring:
  application: internal-testing
  datasource:
    url: jdbc:mysql://localhost:3306/integration_test?createDatabaseIfNotExist=true
    username: root
    password: NewPassword123!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
